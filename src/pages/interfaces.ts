export interface summaryData {
  name: string;
  summary: string;
  temperature: string;
  time: string;
  currency: string;
  rate: string;
  timediff: string;
  timezone: string;
  currencyname: string;
  distance: number;
  flighttime: number;
}

export interface addressData {
  place: string;
}

export interface autocompleteData {
  query: string;
}
